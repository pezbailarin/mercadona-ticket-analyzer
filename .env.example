# ── Cuenta de Gmail ─────────────────────────────────────────────
EMAIL_ADDR=tu_correo@gmail.com

# ── Autenticación (elige uno de los dos métodos) ────────────────

# Método 1: Contraseña de aplicación de Google (más sencillo)
# Genera una en: https://myaccount.google.com/apppasswords
# ponla en la línea siguiente tal cual, sin comillas y 
# con los espacios cada cuatro caracteres.
APP_PASSWORD=

# Método 2: OAuth2 (recomendado; ver CONFIGURAR_OAUTH.md)
# Ruta al credentials.json descargado de Google Cloud Console
# GOOGLE_CREDENTIALS=/ruta/al/proyecto/credentials.json
# Ruta donde se guarda el token entre sesiones (opcional, por defecto junto a los scripts)
# OAUTH_TOKEN=/ruta/al/proyecto/token.json

# ── IMAP ────────────────────────────────────────────────────────
# Carpeta donde están todos los correos.
# Gmail en español: [Google Mail]/Todos
# Gmail en inglés:  [Gmail]/All Mail
IMAP_FOLDER=[Google Mail]/Todos

# ── Rutas (se recomiendan absolutas, especialmente para cron) ───
SAVE_DIR=/ruta/al/proyecto/tickets_nuevos
PROCESSED_DIR=/ruta/al/proyecto/tickets_procesados
ERROR_DIR=/ruta/al/proyecto/tickets_error
DB_PATH=/ruta/al/proyecto/mercadona.db
OUTPUT_DIR=/ruta/al/proyecto

# ── Logs y backups ──────────────────────────────────────────────
LOG_DIR=/ruta/al/proyecto/logs
LOG_RETENTION_DAYS=30
BACKUP_DIR=/ruta/al/proyecto/backups
BACKUP_COUNT=30
